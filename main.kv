#:include servicesmapview.kv

BoxLayout:
    orientation: 'vertical'
    MDToolbar:
        title: 'Services Map'
        md_bg_color: app.theme_cls.primary_color
        right_action_items: [['magnify', lambda x: app.search_pop.open()]]
    ServicesMapView: